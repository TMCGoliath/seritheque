{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} :: Test{% endblock %}

{% block body %}
    <section>
        <div class="flex_between">
            <a href={{ path('app_serie_list') }}
        </div>
    </section>

    {% for index, jour in jours %}
        {{ index ~ ' ' ~ jour ~ ', ' }}
    {% endfor %}

    <br>
    {{ bidule|default('y a pas de bidule') }}
<br>
    {{ varHtml|raw }}


    {% for element in tab %}
        {{ element }}
    {% else %}
        Pas d'élément dans tab, ce tableau est vide
    {% endfor %}



{% endblock %}
